import {
  PayPalButtons,
  PayPalScriptProvider,
  usePayPalScriptReducer,
} from "@paypal/react-paypal-js";
import {
  approvePayPalOrder,
  createPayPalOrder,
} from "@/lib/actions/order.actions";

const OrderDetailsForm = () => {
  return <div>order-details-form</div>;
};

export default OrderDetailsForm;
